/**
 * Demo LPG shortage reports are defined in `convex/reports.ts` (DEMO_REPORTS).
 *
 * To insert all rows into your Convex database, from the `lpg-monitor` folder run:
 *
 *   npm run seed
 *
 * or:
 *
 *   npx convex run reports:seedReports
 *
 * Requires Convex to be configured (e.g. `npx convex dev` at least once).
 */

export {};
